#include "DesignFeedforwardGRN.h"
/*
int f(double * x)
{
	int m[3][3] = {
	               {0,  0,  0},
	               {1,  0,  1},
	               {0,  1,  0}
	            };
	return m[ (int)x[1] ][ (int)x[0] ];
}

int main()
{
	FILE * file;
	int gridsz[] = {3,3};
	setGridSizes(gridsz);
	setTargetFunction(&f);
	
	if (file = fopen("out.dot","w"))
	{
		generateGraphFile(file);
		fclose(file);
	}
}

*/

